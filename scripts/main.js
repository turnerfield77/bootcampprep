// querySelector() grabs the referenced element in the HTML document to apply the javascript code to 
// we stored this in the variable myHeading 
// we then set myHeading variable textContent to 'Hello World'

const myHeading = document.querySelector('h1');
myHeading.textContent = 'Hello world!';